<Window x:Class="Rental.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rental"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
    WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <Button Content="додати авто" Margin="10" Click="Add"/>
            <Button Content="видалити авто" Margin="10" Click="Delete"/>
            <Button Content="зберегти і закрити" Margin="10" Click="Save_Close"/>
            <Button Content="не зберігати і закрити" Margin="10" Click="Cancel_Close"/>
            <Button Content="список авто" Margin="10" Click="List_Auto"/>
            <Button Content="Редагувати" Click="Edit_Click" Margin="5"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="vehicleListBox" Visibility="Collapsed"/>
        </ScrollViewer>
    </Grid>
</Window>
